/* ============================================================
   마인드맵 — Firebase 설정
   ------------------------------------------------------------
   이 파일은 딱 한 번만 채우면 됩니다.
   앞으로 mindmap.html 을 새 버전으로 바꿔도 이 파일은 건드리지
   마세요. 설정이 그대로 유지됩니다.

   값 찾는 곳:
   Firebase 콘솔 → ⚙ 프로젝트 설정 → 일반 →
   "내 앱" 의 웹 앱 → SDK 설정 및 구성 → 구성(Config)

   따옴표 안의 내용만 바꾸고, 쉼표와 중괄호는 그대로 두세요.
   ============================================================ */

window.FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
